{
  "routes": [
    {
      "method": "GET",
      "path": "/",
      "handler": "pages.index",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/body",
      "handler": "pages.body",
      "config": {
        "policies": []
      }
    }
  ]
}
